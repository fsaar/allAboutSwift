import Quick
import Nimble

@testable import smebup

class SBCameraListCellSpecs: QuickSpec {

    override func spec() {
        
        pending("should initialise cell descriptionlabel with cameralist description") {
            
        }
        
        pending("should handle invalid URL gracefully") {
            
        }
        
        pending("should request image if corrrect URL given") {
            
        }
        
        pending("should not request image if invalid URL given") {
            
        }
        
    }

}
