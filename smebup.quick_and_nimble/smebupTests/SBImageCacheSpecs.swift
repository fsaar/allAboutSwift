import Quick
import Nimble

@testable import smebup


class SBImageCacheSpecs: QuickSpec {

    override func spec() {
        

        pending("should ask requestmanager for not cached image") {
            
        }
        
        pending("should call completionblock immediately if url invalid") {
            
        }
        
        pending("should not ask requestmanager for not cache image and consult cache instead") {
            
        }
    }

}
